import React, { Component } from "react";
import PropTypes from "prop-types";

class Plantilla extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return (
      <div>
        <p>Probando apenas section plantilla</p>
      </div>
    );
  }
}

Plantilla.propTypes = {};

export default Plantilla;
